# Images

This folder is reserved for deployable image assets such as a portrait, project photos, diagrams, or screenshots.

The current page uses CSS-native geometric visuals so it can be deployed directly without external image files.
